"""Evidence vault (Epic B): hashing, write-gate, manifest, registries, policy.

Import from the submodules directly (e.g.
``from siftmesh_core.evidence.hash_utils import sha256_file``) to keep this
package marker free of import churn as the epic is built out.
"""

from __future__ import annotations
